import { ResturantEditGuard } from './resturant-edit-guard';

describe('ResturantEditGuard', () => {
  it('should create an instance', () => {
    expect(new ResturantEditGuard()).toBeTruthy();
  });
});
